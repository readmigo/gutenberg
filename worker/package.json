{
  "name": "worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply gutenberg-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply gutenberg-db --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.38",
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "drizzle-kit": "^0.30",
    "typescript": "^5",
    "wrangler": "^4.66.0"
  }
}
